import { StyleSheet, Text, View } from "react-native";
import React from "react";

const ParamAlert = () => {
  return (
    <View>
      <Text>ParamAlert</Text>
    </View>
  );
};

export default ParamAlert;

const styles = StyleSheet.create({});
